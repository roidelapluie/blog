{% extends "base.html" %}
{% block head %}
  {{ super() }}
  {% for keyword in article.keywords %}
    <meta name="keywords" contents="{{keyword}}" />
  {% endfor %}

  {% for description in article.description %}
    <meta name="description" contents="{{description}}" />
  {% endfor %}

{% endblock %}

{% block content %}
.container
  %h1
    <a href="{{ SITEURL }}/{{ article.url }}" rel="bookmark" title="Permalink to {{ article.title|striptags }}">{{ article.title }}</a>
  {{ article.content }}
  {% include "article_infos.html"%}
.prefooter
  .container
    <h5>About</h5>
    %p
      This article titled "{{article.title}}" is part of my personal blog. The <a href="https://github.com/roidelapluie/blog">source code</a> is available on <a href="https://github.com">github</a>. If you find a mistake, a typographical error or any other nonsense, you can send me a <a href="https://github.com/roidelapluie/blog/compare">pull request</a>. This is the revision <a href="changelog.html">{{article.commits|length}}</a> of this post. The <a href="source.html">source</a> is written in Markdown.
{% endblock %}
